# Production server port. 3000 is common locally; use a free port on shared VPSes.
PORT=3102

# Local Python Miruro-API extractor that uses curl_cffi TLS fingerprinting.
# Start it separately, then the Node server calls it for episodes/sources.
MIRURO_API_URL=http://127.0.0.1:8788
